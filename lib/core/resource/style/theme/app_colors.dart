import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color backgroundPrimary = Color(0xFFFFFFFF);
  static const Color backgroundSecondary = Color(0xFFECF8F2);

  // Primary accent
  static const Color primary = Color(0xFF4BBE85);
  static const Color borederEnable = Color(0xFFCCCCCC);
  static const Color borederFoucs = Color(0xFF8FD6B3);
  static const Color borederError = Color(0xFFF90D21);
  static const Color hintColor = Color(0xFF666666);

  // Combined usage for convenience
  static const Color textAndIconPrimary = Color(0xFF0D0D0D);

  static const Color textAndIconSecondary = Color(0xFF29704D);

  static const Color textAndIconThritly = Color(0xFFFFFFFF);
  static const Color textAndIconBlue = Color(0xFF2865FF);

  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color red = Colors.red;

  static const Color shimmerBase = Color(
    0xFF1A1A1A,
  ); // equivalent to Colors.grey[850]
  static const Color shimmerHighlight = Color(
    0xFF424242,
  ); // equivalent to Colors.grey[800]
}
